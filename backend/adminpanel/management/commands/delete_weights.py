# from django.core.management.base import BaseCommand
# from adminpanel.models import Weight  # Replace `myapp` with the actual name of your app

# class Command(BaseCommand):
#     help = 'Deletes all entries in the Weight model'

#     def handle(self, *args, **options):
#         num_deleted, _ = Weight.objects.all().delete()
#         self.stdout.write(self.style.SUCCESS(f'Successfully deleted {num_deleted} weights'))
